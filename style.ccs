/* =========================================
   MILLY SWEET AND FRESH
   ESTILOS PRINCIPALES
========================================= */


/* =========================================
   VARIABLES
========================================= */

:root {
    --morado: #8d4aa8;
    --morado-oscuro: #5f2875;
    --morado-claro: #ead7f0;
    --rosado: #f3c8d8;
    --crema: #fffaf7;
    --blanco: #ffffff;
    --texto: #342b35;
    --gris: #756d75;

    --sombra: 0 10px 30px rgba(70, 35, 80, 0.12);

    --radio: 20px;

    --transicion: 0.3s ease;
}


/* =========================================
   RESET
========================================= */

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

html {
    scroll-behavior: smooth;
}

body {
    font-family: "Poppins", sans-serif;
    background: var(--crema);
    color: var(--texto);
    line-height: 1.6;
}

img {
    width: 100%;
    display: block;
    object-fit: cover;
}

a {
    text-decoration: none;
    color: inherit;
}

button {
    font-family: inherit;
}


/* =========================================
   CONTENEDOR
========================================= */

.container {
    width: min(1120px, 90%);
    margin: 0 auto;
}


/* =========================================
   HEADER
========================================= */

.header {
    position: sticky;
    top: 0;
    z-index: 1000;

    background: rgba(255, 250, 247, 0.95);

    backdrop-filter: blur(12px);

    border-bottom: 1px solid rgba(141, 74, 168, 0.08);
}

.navbar {
    min-height: 80px;

    display: flex;
    align-items: center;
    justify-content: space-between;
}


/* LOGO */

.logo {
    display: flex;
    flex-direction: column;

    line-height: 1;
}

.logo span {
    font-family: "Playfair Display", serif;

    font-size: 2rem;
    font-weight: 700;

    color: var(--morado);
}

.logo small {
    margin-top: 5px;

    font-size: 0.62rem;
    letter-spacing: 3px;

    color: var(--gris);

    text-transform: uppercase;
}


/* NAV */

.nav-menu {
    display: flex;
    align-items: center;

    gap: 35px;

    list-style: none;
}

.nav-menu a {
    position: relative;

    font-size: 0.9rem;
    font-weight: 500;

    transition: var(--transicion);
}

.nav-menu a::after {
    content: "";

    position: absolute;

    left: 0;
    bottom: -7px;

    width: 0;
    height: 2px;

    background: var(--morado);

    transition: var(--transicion);
}

.nav-menu a:hover {
    color: var(--morado);
}

.nav-menu a:hover::after {
    width: 100%;
}


/* BOTÓN MENÚ */

.menu-toggle {
    display: none;

    border: none;
    background: none;

    font-size: 1.7rem;

    cursor: pointer;

    color: var(--morado);
}


/* =========================================
   HERO
========================================= */

.hero {
    min-height: calc(100vh - 80px);

    display: flex;
    align-items: center;

    padding: 80px 0;
}

.hero-content {
    display: grid;

    grid-template-columns: 1fr 1fr;

    gap: 70px;

    align-items: center;
}


/* TEXTO */

.hero-subtitle,
.section-subtitle {
    color: var(--morado);

    font-size: 0.75rem;

    font-weight: 700;

    letter-spacing: 3px;
}

.hero h1 {
    margin: 18px 0;

    font-family: "Playfair Display", serif;

    font-size: clamp(3rem, 6vw, 5.3rem);

    line-height: 1.05;

    font-weight: 600;
}

.hero h1 span,
.section-header h2 span,
.about-text h2 span {
    display: block;

    color: var(--morado);
}

.hero-description {
    max-width: 540px;

    color: var(--gris);

    font-size: 1rem;
}


/* BOTONES */

.hero-buttons {
    display: flex;

    gap: 15px;

    flex-wrap: wrap;

    margin-top: 35px;
}

.btn {
    display: inline-flex;

    align-items: center;
    justify-content: center;

    min-height: 50px;

    padding: 0 25px;

    border-radius: 50px;

    font-size: 0.85rem;

    font-weight: 600;

    transition: var(--transicion);
}

.btn-primary {
    background: var(--morado);

    color: var(--blanco);

    box-shadow: 0 8px 20px rgba(141, 74, 168, 0.25);
}

.btn-primary:hover {
    background: var(--morado-oscuro);

    transform: translateY(-3px);
}

.btn-secondary {
    border: 1px solid var(--morado);

    color: var(--morado);
}

.btn-secondary:hover {
    background: var(--morado);

    color: var(--blanco);

    transform: translateY(-3px);
}


/* IMAGEN HERO */

.hero-image {
    position: relative;

    max-width: 500px;

    margin-left: auto;
}

.hero-image img {
    position: relative;

    height: 540px;

    border-radius: 45% 45% 20px 20px;

    object-fit: cover;

    box-shadow: var(--sombra);

    z-index: 2;
}

.hero-decoration {
    position: absolute;

    width: 220px;
    height: 220px;

    right: -45px;
    top: -45px;

    background: var(--rosado);

    border-radius: 50%;

    z-index: 1;
}


/* =========================================
   SECCIONES
========================================= */

.featured,
.gallery {
    padding: 110px 0;
}

.section-header {
    max-width: 650px;

    margin: 0 auto 55px;

    text-align: center;
}

.section-header h2 {
    margin: 12px 0 15px;

    font-family: "Playfair Display", serif;

    font-size: clamp(2.5rem, 5vw, 4rem);

    line-height: 1.1;
}

.section-header > p:last-child {
    color: var(--gris);

    font-size: 0.95rem;
}


/* =========================================
   PRODUCTOS
========================================= */

.products-grid {
    display: grid;

    grid-template-columns: repeat(3, 1fr);

    gap: 25px;
}

.product-card {
    overflow: hidden;

    background: var(--blanco);

    border-radius: var(--radio);

    box-shadow: var(--sombra);

    transition: var(--transicion);
}

.product-card:hover {
    transform: translateY(-8px);

    box-shadow: 0 18px 40px rgba(70, 35, 80, 0.18);
}

.product-image {
    overflow: hidden;

    height: 270px;
}

.product-image img {
    height: 100%;

    transition: transform 0.5s ease;
}

.product-card:hover .product-image img {
    transform: scale(1.06);
}

.product-info {
    padding: 25px;
}

.product-tag {
    display: inline-block;

    margin-bottom: 10px;

    padding: 5px 12px;

    border-radius: 50px;

    background: var(--morado-claro);

    color: var(--morado);

    font-size: 0.7rem;

    font-weight: 600;
}

.product-info h3 {
    margin-bottom: 10px;

    font-family: "Playfair Display", serif;

    font-size: 1.7rem;
}

.product-info p {
    margin-bottom: 20px;

    color: var(--gris);

    font-size: 0.85rem;
}

.product-info a {
    color: var(--morado);

    font-size: 0.85rem;

    font-weight: 700;
}

.product-info a:hover {
    color: var(--morado-oscuro);
}


/* =========================================
   SOBRE NOSOTROS
========================================= */

.about {
    padding: 110px 0;

    background: var(--morado-claro);
}

.about-content {
    display: grid;

    grid-template-columns: 0.8fr 1.2fr;

    gap: 80px;

    align-items: center;
}

.about-image {
    display: flex;

    align-items: center;
    justify-content: center;
}

.about-image img {
    width: 280px;
    height: 280px;

    object-fit: contain;

    border-radius: 50%;
}

.about-text h2 {
    margin: 15px 0 25px;

    font-family: "Playfair Display", serif;

    font-size: clamp(2.5rem, 5vw, 4rem);

    line-height: 1.1;
}

.about-text p {
    max-width: 600px;

    margin-bottom: 18px;

    color: var(--gris);

    font-size: 0.95rem;
}

.about-text .btn {
    margin-top: 15px;
}


/* =========================================
   GALERÍA
========================================= */

.gallery {
    background: var(--crema);
}

.gallery-grid {
    display: grid;

    grid-template-columns: repeat(3, 1fr);

    gap: 20px;
}

.gallery-grid img {
    height: 350px;

    border-radius: var(--radio);

    box-shadow: var(--sombra);

    transition: var(--transicion);
}

.gallery-grid img:hover {
    transform: scale(1.02);
}


/* =========================================
   CONTACTO
========================================= */

.contact {
    padding: 100px 0;

    background: var(--morado);
}

.contact-content {
    display: flex;

    align-items: center;
    justify-content: space-between;

    gap: 50px;
}

.contact-text {
    color: var(--blanco);
}

.contact-text .section-subtitle {
    color: var(--rosado);
}

.contact-text h2 {
    margin: 12px 0;

    font-family: "Playfair Display", serif;

    font-size: clamp(2.5rem, 5vw, 4rem);

    line-height: 1.1;
}

.contact-text p:last-child {
    max-width: 500px;

    color: rgba(255, 255, 255, 0.8);
}

.contact-buttons {
    display: flex;

    flex-direction: column;

    gap: 12px;

    min-width: 230px;
}

.contact-buttons .btn {
    background: var(--blanco);

    color: var(--morado);
}

.contact-buttons .btn:hover {
    transform: translateY(-3px);

    background: var(--rosado);
}


/* WHATSAPP */

.btn-whatsapp {
    font-weight: 700;
}


/* =========================================
   FOOTER
========================================= */

.footer {
    padding: 35px 0;

    background: var(--morado-oscuro);

    color: var(--blanco);
}

.footer-content {
    display: flex;

    align-items: center;
    justify-content: space-between;

    gap: 30px;
}

.footer-logo h3 {
    font-family: "Playfair Display", serif;

    font-size: 1.8rem;
}

.footer-logo p {
    color: var(--rosado);

    font-size: 0.7rem;

    letter-spacing: 2px;

    text-transform: uppercase;
}

.footer-info {
    text-align: right;

    color: rgba(255, 255, 255, 0.7);

    font-size: 0.75rem;
}


/* =========================================
   RESPONSIVE
========================================= */

@media (max-width: 900px) {

    .hero-content {
        grid-template-columns: 1fr;

        gap: 50px;
    }

    .hero {
        padding: 60px 0;
    }

    .hero-image {
        margin: 0 auto;

        width: min(100%, 500px);
    }

    .products-grid {
        grid-template-columns: 1fr 1fr;
    }

    .about-content {
        grid-template-columns: 1fr;

        text-align: center;
    }

    .about-image {
        order: 2;
    }

    .about-text p {
        margin-left: auto;
        margin-right: auto;
    }

    .contact-content {
        flex-direction: column;

        text-align: center;
    }

    .contact-text p:last-child {
        margin: auto;
    }

    .contact-buttons {
        width: 100%;
        max-width: 350px;
    }
}


@media (max-width: 700px) {

    .navbar {
        min-height: 70px;
    }

    .menu-toggle {
        display: block;
    }

    .nav-menu {
        position: absolute;

        top: 70px;
        left: 0;

        width: 100%;

        padding: 25px;

        flex-direction: column;

        gap: 20px;

        background: var(--crema);

        box-shadow: var(--sombra);

        transform: translateY(-150%);

        transition: var(--transicion);
    }

    .nav-menu.active {
        transform: translateY(0);
    }

    .hero h1 {
        font-size: 3rem;
    }

    .hero-image img {
        height: 420px;
    }

    .products-grid {
        grid-template-columns: 1fr;
    }

    .gallery-grid {
        grid-template-columns: 1fr;
    }

    .gallery-grid img {
        height: 300px;
    }

    .footer-content {
        flex-direction: column;

        text-align: center;
    }

    .footer-info {
        text-align: center;
    }
}


@media (max-width: 450px) {

    .container {
        width: 88%;
    }

    .hero {
        padding: 45px 0;
    }

    .hero h1 {
        font-size: 2.6rem;
    }

    .hero-buttons {
        flex-direction: column;
    }

    .hero-buttons .btn {
        width: 100%;
    }

    .hero-image img {
        height: 350px;
    }

    .hero-decoration {
        width: 140px;
        height: 140px;

        right: -20px;
        top: -20px;
    }

    .featured,
    .gallery,
    .about {
        padding: 75px 0;
    }

}
